根据 gemini.txt 和 fix.txt 文件，实施以下优化：

1. **增强 TripIntent 数据结构** - 添加 hotel_intent（三级状态）、special_requirements、budget_level、travel_style、has_conflict 等字段

2. **重写 IntentAnalyzer** - 使用新的 TripIntent 模型和 INTENT_PROMPT，支持语义理解而非关键词匹配

3. **修改 Supervisor** - 从 trip_intent 读取意图，不再自己推断，职责分离为意图层和运行时层

4. **修改 Planner** - 移除二次字符串截断，改用数据驱动的字段过滤，注入 trip_intent 约束

5. **验证所有修改** - 确保导入和运行正常