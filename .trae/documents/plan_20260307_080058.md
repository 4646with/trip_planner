## 重构计划：根据 Gemini 方案重构 map_agents.py

### 主要变更

1. **移除 `AgentNodes` 类** - 将节点方法整合到 `MapAgentsSystem` 类中

2. **添加通用 `_worker_node` 方法** - 统一处理所有 Worker 节点的执行逻辑

3. **重构 `MapAgentsSystem` 类**：
   - 添加 `_agents` 字典缓存 agent 实例
   - 添加 `_api_semaphore` 实例变量
   - 添加 `_get_tools()` 实例方法
   - 将 `supervisor_node`、`attraction_node`、`weather_node`、`hotel_node`、`route_node`、`planner_node` 改为实例方法

4. **简化模块级函数** - 保留 `get_map_agents_system()` 和 `initialize_map_agents_system()` 作为入口

5. **保留现有功能** - 所有数据处理方法（`clean_llm_json_data`、`_parse_json_to_plan`、`_build_trip_plan`）保持不变