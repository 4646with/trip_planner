## 重构计划概述

基于代码审查，发现以下问题需要修复：

### Phase 1: 高优先级（消除死代码和矛盾）

| # | 问题 | 修复方案 | 位置 |
|---|---|---|---|
| 1 | `RETRY_ERROR_HINTS`、`_classify_error` 死代码 | 删除未使用的变量和函数 | `agent_output.py` |
| 2 | `SearchResults` 模型从未使用 | 删除未使用的模型 | `agent_output.py` |
| 3 | `intent_based_router` 与新架构矛盾 | 删除函数 + `smart_router` 的 `total_calls==0` 分支 | `rule_router.py` |
| 4 | Supervisor LLM chain 永不执行 | 简化为纯包装，只调用 `fault_check_router` | `supervisor.py` |
| 5 | 同步 `plan_trip` 方法无调用方 | 删除同步方法 | `main.py` |
| 6 | `parse_and_validate` 未使用 | 删除未使用的函数 | `workers.py` |
| 7 | `get_all_node_funcs` 方法无调用方 | 删除未使用的方法 | `workers.py` |

### Phase 2: 中优先级（清理和优化）

| # | 问题 | 修复方案 | 位置 |
|---|---|---|---|
| 8 | `MAX_AGENT_CALLS` 两处值不同（6 vs 10） | 统一为 6，移到共享常量文件 | `rule_router.py`, `supervisor.py` |
| 9 | `with_retry_and_log` 重试 `max_retries=3` 可能阻塞 | 改为 `max_retries=1`，仅限流重试 | `workers.py` |
| 10 | PLANNER prompt 有矛盾住宿说明 | 删除旧版遗留说明 | `agents.py` |

### Phase 3: 低优先级（架构优化）

| # | 问题 | 修复方案 | 位置 |
|---|---|---|---|
| 11 | `rule_router.py` 和 `supervisor.py` 职责重叠 | 合并为单一文件 `router.py` | 新建文件 |

### 预估效果
- 代码行数减少：~200+ 行
- 消除死代码：多处
- 路由逻辑：更清晰统一

### 执行顺序
1. Phase 1: 删除死代码和矛盾逻辑
2. Phase 2: 统一常量和优化
3. Phase 3: 架构合并（可选）

请确认此计划后，我将开始执行重构。